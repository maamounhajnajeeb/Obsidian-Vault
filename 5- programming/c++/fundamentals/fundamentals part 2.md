#### define a set:
```cpp
#include <set>
set<char> myUniqueSet;
```
and it has multiple functions like: insert(), erase() and size()
#### sort an array:
```cpp
std::sort(arrayItself, arrayItself + arraySize);
```
